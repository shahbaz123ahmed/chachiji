import { Product } from "@/types/ecommerce";

// Fallback exported constant (zero hardcoded products)
export const PRODUCTS: Product[] = [];

export function getAllProducts(): Product[] {
  return [];
}
